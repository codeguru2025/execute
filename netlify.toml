[build]
  command = "npx vite build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"

# wouter client-side routing needs deep links to fall back to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
